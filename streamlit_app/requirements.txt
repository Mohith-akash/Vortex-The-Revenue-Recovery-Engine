# Vortex Streamlit App Requirements
# All versions pinned so cloud rebuilds are deterministic

# Core
streamlit==1.59.1
pandas==2.3.3
plotly==6.9.0

# Data generation
faker==40.28.1

# AI Services (optional - falls back to templates if not available)
cerebras-cloud-sdk==1.67.0
voyageai==0.5.0

# Azure Event Hub (for production streaming)
azure-eventhub==5.15.1

# Utilities
python-dotenv==1.2.2
